git config --global  user.name "zhangnkai"
git config --global user.email "837861802@qq.com"
git  init
git add readme2.txt    将文件提交到仓库,暂存区
git commit -m "my first code"  暂存区到当前分支
git status   获取当前仓库状态
git diff  readme.txt
git log   查看提交记录
git log --pretty=oneline      简化输出
git reset --hard 35689     回退至指定版本
git reflog   查看历史版本，删除错误可以使用该命令进行回退
git diff  HEAD -- readme2.txt   查看暂存区与分支的关系
git checkout -- readme2.txt   #就是让这个文件回到最近一次git commit或git add时的状态


rm filename  #删除文件
git checkout -- filename  #将版本库的文件还原
git rm filename  #将版本库中的文件删除
git cimmit -m "注释"  #提交修改


创建远程仓库
ssh-keygen -t rsa -C "837861802@qq.com"

git remote add orgin git@github.com:zhangkai-th/learngit.git
git push -u origin master  将本地master推送至远程库
git remote -v  查看当前添加的远程仓库
git remote rm orgin   删除添加的远程仓库

git clone git@github.com:zhangkai-th/gitskills.git  #将远程仓库克隆到本地

分支概念
git checkout -b dev  #1、git branch dev 2 git checkout dev  创建并切换到该分支
git branch  查看当前分支

git checkout dev   切换到dev分支
git checkout master
git merge  dev     合并分支
git branch -d dev  删除分支

git switch -c dev   创建并切换到dev
git switch master   切换到master

解决冲突
git log --graph --pretty=oneline --abbrev-commit  查看分支情况

分支合并策略
git merge --no-ff -m "hshhs" dev    --no-ff 可以查看合并的历史


bug分支
git stash
git stash list
git stash pop
git stash apply stash@{0}
git cherry--pick 4c058e2



放置实验性的代码

 git checkout -b  feature-vulan  创建并合并分支
 git add say1.py
 git commit -m "fe commit"
 git checkout master    切换至master分支
 git merage  feature-vulan  合并分支
 git branch -D feature-vulan  强制删除分支

多人协作
git remote 查看远程库的信息
git remote -v
git push orgin msater 将master分支推送至远程仓库
创建远程分支到本地
git checkout -b dev origin/dev
git push origin dev  将dev推送至远程仓库
git pull   将最新的提交从origin/dev  拉倒本地
git branch --set-upstream-to=origin/dev  dev   设置dev与origin/dev 的链接
git rebase

标签 版本库的镜像
git tag v1.0
git tag 
git log --pretty=oneline --abbrev-commit
git tag v1.0 f52o098  给历史提交记录打标签
git show v1.0   显示详细信息 
git tag -a v1.0 -m "vesion1" f52o098  带有说明的标签
git tag -d v0.1  删除标签
git push origin v1.0  将一个标签推送至远程仓库
git push origin --tags   将所有标签推送至远程仓库
删除远程标签的做法
git tag -d v0.9
git push  origin :refs/tags/v0.9